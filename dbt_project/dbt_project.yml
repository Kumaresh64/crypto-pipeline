name: 'crypto_pipeline'
version: '1.0.0'
config-version: 2

profile: 'crypto_pipeline'

model-paths: ["models"]
test-paths: ["tests"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  crypto_pipeline:
    bronze:
      +materialized: view
    silver:
      +materialized: table
    gold:
      +materialized: table
